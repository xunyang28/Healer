version: '3.8'

services:
  app:
    build: .
    ports:
      - "80:3000"          # browser uses port 80, app runs on 3000 internally
    environment:
      - NODE_ENV=development
      - PORT=3000
    volumes:
      - ./src:/app/src